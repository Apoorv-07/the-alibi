# ALIBI — the whole dependency surface of the product, on purpose.
#
# A student laptop is the deployment target. Every dependency below earns its place by being load-bearing;
# anything else (an ORM, a web framework, a validation library, a charting library) was refused because the
# ledger, the verifier, the solver and the UI are all ~1k lines of Python and hand-drawn SVG. Each line
# here is also a line the graders must trust to work offline.

# deterministic CP-SAT solver — the only component with no reasonable hand-written substitute.
# 9.11.x is no longer distributable (PyPI serves 9.12+); this pin is the newest release whose
# `cp_model` API this file's caller uses unchanged. If you bump it, run `make test` first: the solver
# assertions in tests/test_core.py are the safety net.
ortools==9.15.6755 ; python_version >= "3.10"

# web layer: FastAPI + Jinja + uvicorn. Starlette is pulled in by FastAPI.
fastapi==0.118.0
uvicorn[standard]==0.36.1
jinja2==3.1.6
python-multipart==0.0.20

# tests + the smoke check (httpx is what starlette's TestClient drives)
pytest==8.4.2
httpx==0.28.1
